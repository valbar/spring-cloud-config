spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/valbar/spring-config-server-data
          clone-on-start: true
server:
  port: ${PORT:8060}
